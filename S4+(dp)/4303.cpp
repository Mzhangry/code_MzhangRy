// MzhangRy - 张容钰
#include<iostream>
#include<iomanip>
#include<cmath>
#include<string>
#include<algorithm>
#include<vector>
using namespace std;

int a[1005], b[1005], c[1005];
int f[1005], tim[1005];
int main() {
    int t;
    cin >> t;
    while(t--) {    
        int n, k;
        cin >> n >> k;
        for (int i=1;i<=n;i++) {
            a[i] = 1;
            cin >> b[i];
            f[i] = 0;
            tim[i] = -1;
        }
        for (int i=1;i<=n;i++) cin >> c[i];
        for (int i=1;i<=n;i++) {

        }
    }
}